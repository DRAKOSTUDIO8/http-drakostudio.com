@import url('https://fonts.googleapis.com/css2?family=Playfair+Display:wght@700&family=Rubik+Glitch&family=Inter:wght@400;700&display=swap');

:root {
    --color-primary: #FF0080; /* Un rosa/magenta vibrante para CTAs */
    --color-secondary: #00FFC2; /* Un verde aqua para contraste */
    --color-dark: #1A1A1A;
    --color-light: #F7F7F7;
}

body {
    font-family: 'Inter', sans-serif; /* Usamos una fuente limpia para el cuerpo */
    background-color: var(--color-dark);
    color: var(--color-light);
    margin: 0;
    padding: 0;
}

/* Header y Navegación */
.main-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 20px 5%;
    background-color: #222;
    border-bottom: 3px solid var(--color-secondary); /* Línea de contraste */
}
.logo { max-width: 100px; }
.main-header nav a {
    color: var(--color-light);
    text-decoration: none;
    margin-left: 20px;
    font-weight: 700;
    transition: color 0.3s;
}
.main-header nav a:hover { color: var(--color-primary); }

/* SECCIÓN HERO */
.hero-section {
    padding: 80px 5%;
    text-align: center;
    background: url('fondo-abstracto.jpg') center center / cover; /* Reemplaza con una imagen tuya */
    color: var(--color-light);
}
.glitch-title { 
    font-family: 'Rubik Glitch', cursive; 
    font-size: 50px; 
    color: var(--color-secondary); 
    margin-bottom: 10px;
}
.slogan { 
    font-family: 'Playfair Display', serif;
    font-size: 28px; 
    font-weight: 700;
    margin-bottom: 20px;
}
.subtext { font-size: 16px; max-width: 600px; margin: 0 auto 30px; }

/* Botones Principales (CTA) */
.cta-button {
    background: var(--color-primary);
    color: var(--color-light);
    padding: 15px 30px;
    border: none;
    border-radius: 8px;
    font-size: 20px;
    font-weight: 700;
    cursor: pointer;
    transition: background 0.3s, transform 0.3s;
    text-transform: uppercase;
}
.cta-button:hover {
    background: #ff4d99; /* Tono más claro al pasar el ratón */
    transform: scale(1.05);
}

/* SECCIÓN PRODUCTOS DESTACADOS */
.featured-products {
    padding: 50px 5%;
    text-align: center;
}
.featured-products h2 {
    font-family: 'Playfair Display', serif;
    font-size: 32px;
    margin-bottom: 40px;
    color: var(--color-secondary);
}
.product-card {
    background-color: #2a2a2a;
    padding: 20px;
    margin: 20px;
    border-radius: 10px;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
    display: inline-block; /* Permite que se vean en fila */
    width: 250px; /* Ancho fijo para las tarjetas */
    text-align: left;
}
.product-image {
    width: 100%;
    height: auto;
    border-radius: 5px;
    margin-bottom: 15px;
}
.price {
    font-weight: 700;
    color: var(--color-primary);
    font-size: 22px;
}
.checkout-button {
    /* Mismo estilo que cta-button, pero con un poco de margen */
    background: var(--color-primary);
    color: var(--color-light);
    padding: 15px 30px;
    border: none;
    border-radius: 8px;
    font-size: 20px;
    font-weight: 700;
    cursor: pointer;
    transition: background 0.3s, transform 0.3s;
    text-transform: uppercase;
    margin-top: 40px;
}

/* Pie de Página */
.main-footer {
    background-color: #222;
    padding: 20px 5%;
    font-size: 14px;
    border-top: 3px solid var(--color-secondary);
}
.social-links a {
    color: var(--color-light);
    text-decoration: none;
    margin: 0 10px;
    transition: color 0.3s;
}
.social-links a:hover { color: var(--color-primary); }